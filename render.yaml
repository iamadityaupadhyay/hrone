services:
  - type: worker
    name: hrone-attendance-worker
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run worker
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: test
      - key: TZ
        value: Asia/Kolkata
